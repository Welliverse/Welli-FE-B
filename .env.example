# BE API 준비되면 false로 바꾸고 실제 baseURL 지정
# 실제 서버: http://1.201.116.47:8080 (가비아 배포, main 병합 시 자동 재배포)
# CORS는 localhost:3000만 허용 목록에 있음 — vite dev 서버도 3000 포트로 고정돼 있음
VITE_USE_MOCK=true
VITE_API_BASE_URL=/api
